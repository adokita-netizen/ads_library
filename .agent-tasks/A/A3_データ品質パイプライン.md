# Agent A 大型タスク: データ品質パイプライン

## 概要
176件の広告データの品質を全面的に向上させる。
動画URL抽出・カテゴリ分類・遷移先URL補完・広告生存確認を一括実行。

## 必ず最初に読むファイル
```
backend/scripts/extract_missing_videos.py    ← 動画URL抽出スクリプト
backend/scripts/classify_ads.py              ← カテゴリ分類スクリプト
backend/scripts/fix_destination_urls.py       ← 遷移先URL修復
backend/scripts/check_ad_survival.py          ← 広告生存確認
backend/app/models/ad.py                      ← Adモデル定義
backend/app/services/media_extraction.py      ← MediaExtractor
```

---

## タスク1: カテゴリ分類の実行
**スクリプト:** `backend/scripts/classify_ads.py`

### 概要
category=NULLの広告に対してキーワードベースでカテゴリ分類を実行。
現在176広告のうち、大半がcategory未設定 → ジャンル別分析の精度が低い。

### 修正が必要な場合
- cp932エンコーディング問題防止のため、日本語print文を英語に変更
- `SyncSessionLocal` の import を確認
- 既存スクリプトの実行確認 → 問題があれば修正

### 実行コマンド
```bash
cd C:/Users/ishit/ads_library/backend && python scripts/classify_ads.py
```

---

## タスク2: 遷移先URL補完
**スクリプト:** `backend/scripts/fix_destination_urls.py`

### 概要
destination_url=NULLの広告に対して、ad_metadata・description等からURLを抽出・補完。
LP分析やLP確認ボタンの機能を活かすために必要。

### 修正が必要な場合
- cp932エンコーディング問題のチェック
- `SyncSessionLocal` import確認

### 実行コマンド
```bash
cd C:/Users/ishit/ads_library/backend && python scripts/fix_destination_urls.py
```

---

## タスク3: タイトル補完
**スクリプト:** `backend/scripts/fix_titles.py`

### 概要
title=NULL/空の広告に対して、description・advertiser_name等からタイトルを生成。

### 実行コマンド
```bash
cd C:/Users/ishit/ads_library/backend && python scripts/fix_titles.py
```

---

## タスク4: delivery_dates の再収集
**スクリプト:** `backend/scripts/collect_delivery_dates.py`

### 概要
タスク1-3で更新されたデータに基づいて、days_running・推定インプレッション・推定消化額を再計算。
前回実行済みだが、カテゴリ分類後に再実行すると推定精度が向上する。

### 実行コマンド
```bash
cd C:/Users/ishit/ads_library/backend && python scripts/collect_delivery_dates.py
```

---

## タスク5: ヒットスコア再計算
**スクリプト:** `backend/scripts/recompute_hit_scores.py`

### 概要
タスク1-4で更新されたデータを反映してヒットスコアを再計算。
特にカテゴリ分類完了後はジャンル別統計の精度が大幅に向上する。

### 実行コマンド
```bash
cd C:/Users/ishit/ads_library/backend && python scripts/recompute_hit_scores.py
```

---

## コンフリクト防止ルール
- `INSTRUCTIONS.md` を厳守
- 修正するファイル: `backend/scripts/` 内のスクリプトのみ
- `backend/app/services/ranking/` は触らない（Agent C領域）
- `frontend/` は触らない（Agent B領域）
- 全スクリプトの print 文は英語のみ（cp932エンコーディング問題防止）
  - NG: `print("カテゴリ分類完了")`
  - OK: `print("Category classification complete")`
- `flag_modified(ad, "ad_metadata")` を必ず使う

## 完了条件
- 5スクリプト全てが正常に実行される（exit code 0）
- エンコーディングエラーが発生しない
- category=NULL の広告数が大幅に減少
- destination_url=NULL の広告数が減少
- title=NULL の広告数がゼロに近づく
