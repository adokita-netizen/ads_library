# B51: Full-Cycle Frontend Taskpack (Implement + Test + Fix)

## スコープ
UI側で「実装→ブラウザ検証→不具合修正」を1タスクで完了させる。

## 実装
1. 判定根拠/乖離レビューUI
2. 手動クロール後の即時反映UX
3. 辞書レビュー操作UI

## テスト
1. Playwright: クロール→反映→再分類
2. Playwright: 失敗時表示（500/timeout）
3. 視認性: 低解像度画像の検出表示

## 修正
1. UIクラッシュ/未反映の再発ケース潰し
2. エラーメッセージ統一
3. フィルタ同期崩れ修正

## 完了条件
- [ ] 実装が反映される
- [ ] ブラウザテストが通る
- [ ] 主要不具合が修正される

