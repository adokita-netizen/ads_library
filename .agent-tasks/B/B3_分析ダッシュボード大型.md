# Agent B 大型タスク: 分析ダッシュボード強化

## 概要
ヒット広告分析ビュー (HitAdAnalysisView.tsx) を大幅に強化する。
スコア内訳のビジュアル化、高度なフィルタリング、スコア分布チャートを実装する。

## 必ず最初に読むファイル
```
frontend/src/components/dashboard/HitAdAnalysisView.tsx  ← 主要な修正対象
frontend/src/components/dashboard/HitAdCardView.tsx      ← カードビューコンポーネント
frontend/src/components/analysis/ProductDetailModal.tsx   ← モーダルの既存実装
frontend/src/lib/api.ts                                  ← fetchApi関数
frontend/src/lib/constants.ts                            ← ジャンル・プラットフォーム定数
frontend/src/lib/format.ts                               ← formatNumber, formatYen
frontend/src/types/index.ts                              ← 型定義
```

---

## タスク1: スコア内訳バーチャート (HitAdAnalysisView.tsx テーブル拡張)

### 概要
テーブルの各行にスコア内訳を横棒グラフで表示する。ライブラリ不要、CSS + divで実装。

### 表示仕様
「スコア内訳」カラムを追加。各シグナルの寄与度を色付きバーで表示:
- longevity (青): max 40pt
- spend (緑): max 20pt
- active_bonus (オレンジ): max 20pt
- creative (紫): max 10pt
- trend (ピンク): max 10pt

```tsx
// スコア内訳バーの例
<td className="px-2 py-1">
  <div className="flex items-center gap-0.5 h-3">
    {/* 各シグナルをmax比で幅計算 */}
    <div className="h-full bg-blue-400 rounded-sm"
         style={{ width: `${(signals.longevity / 40) * 100}%` }}
         title={`配信継続力: ${signals.longevity}/40`} />
    <div className="h-full bg-green-400 rounded-sm"
         style={{ width: `${(signals.spend / 20) * 50}%` }}
         title={`消化額: ${signals.spend}/20`} />
    {/* ... */}
  </div>
  <p className="text-[9px] text-gray-400 mt-0.5">{ad.hit_score}/100</p>
</td>
```

### データ取得
- APIレスポンスに `score_breakdown` がある場合はそれを使用
- ない場合は `hit_score` のみ表示（バーなし）
- HitAd interfaceに `score_breakdown?: Record<string, number>` を追加

---

## タスク2: 高度なフィルタリングUI

### 概要
テーブルヘッダー下に折りたたみ式フィルターバーを追加。

### フィルター項目
1. **スコア範囲**: 2つの数値入力 (min/max) またはプリセットボタン
   - プリセット: 「全て」「大HIT (70+)」「HIT (45+)」「低スコア (0-44)」
2. **配信日数範囲**: 2つの数値入力 (min/max) またはプリセット
   - プリセット: 「全期間」「30日以上」「60日以上」「90日以上」
3. **配信状態**: セレクト「全て / 配信中 / 停止済み」
4. **ソート順**: セレクト「スコア順 / 配信日数順 / 消化額順 / 最新順」

### 実装方針
```tsx
const [filters, setFilters] = useState({
  scoreMin: 0,
  scoreMax: 100,
  daysMin: 0,
  daysMax: 9999,
  runningStatus: "all" as "all" | "running" | "stopped",
  sortBy: "score" as "score" | "days" | "spend" | "recent",
});

// フィルター・ソート適用
const filteredAds = useMemo(() => {
  let result = hitAds.filter(ad => {
    if (ad.hit_score < filters.scoreMin || ad.hit_score > filters.scoreMax) return false;
    if ((ad.days_running || 0) < filters.daysMin) return false;
    if ((ad.days_running || 0) > filters.daysMax) return false;
    if (filters.runningStatus === "running" && !ad.is_still_running) return false;
    if (filters.runningStatus === "stopped" && ad.is_still_running) return false;
    return true;
  });
  // ソート
  switch (filters.sortBy) {
    case "score": result.sort((a, b) => (b.hit_score || 0) - (a.hit_score || 0)); break;
    case "days": result.sort((a, b) => (b.days_running || 0) - (a.days_running || 0)); break;
    case "spend": result.sort((a, b) => (b.spend_increase || 0) - (a.spend_increase || 0)); break;
    case "recent": result.sort((a, b) => (b.days_running || 0) - (a.days_running || 0)); break;
  }
  return result;
}, [hitAds, filters]);
```

### UI配置
フィルターバーは折りたたみ式:
```tsx
<div className="flex items-center gap-2">
  <button onClick={() => setShowFilters(!showFilters)} className="...">
    フィルター {showFilters ? "▲" : "▼"}
  </button>
  {/* アクティブなフィルター数を表示 */}
  {activeFilterCount > 0 && <span className="badge">フィルター: {activeFilterCount}</span>}
</div>
```

---

## タスク3: スコア分布ミニチャート（サマリーカード内）

### 概要
サマリーカードエリア（grid grid-cols-3の下）にスコア分布の棒グラフを追加する。
6列目のサマリーカードとして配置。

### 実装仕様
- 10本のバー（0-9, 10-19, ..., 90-100）をCSS divで描画
- X軸ラベル: 0, 20, 40, 60, 80, 100
- 各バーの高さ = (バケットのcount / maxCount) * 100%
- ヒット閾値（45点）に縦の点線を表示

```tsx
// サマリーカードエリアの後に追加
<div className="card px-4 py-3">
  <p className="text-[11px] text-gray-400 font-medium">スコア分布</p>
  <div className="flex items-end gap-px h-12 mt-2">
    {scoreBuckets.map((bucket, i) => (
      <div key={i} className="flex-1 flex flex-col items-center">
        <div
          className={`w-full rounded-t-sm ${bucket.range >= 70 ? "bg-red-400" : bucket.range >= 45 ? "bg-orange-400" : "bg-gray-300"}`}
          style={{ height: `${(bucket.count / maxBucketCount) * 100}%`, minHeight: bucket.count > 0 ? "2px" : "0" }}
        />
      </div>
    ))}
  </div>
  <div className="flex justify-between text-[8px] text-gray-400 mt-1">
    <span>0</span><span>50</span><span>100</span>
  </div>
</div>
```

### データ計算
```tsx
const scoreBuckets = useMemo(() => {
  const buckets = Array.from({ length: 10 }, (_, i) => ({
    range: i * 10,
    count: 0,
  }));
  hitAds.forEach(ad => {
    const idx = Math.min(9, Math.floor((ad.hit_score || 0) / 10));
    buckets[idx].count++;
  });
  return buckets;
}, [hitAds]);
const maxBucketCount = Math.max(1, ...scoreBuckets.map(b => b.count));
```

---

## タスク4: 広告主別集約ビュー（新タブ）

### 概要
ヒット広告分析ビューのヘッダーに「テーブル / カード / 広告主別」の3つ目のタブを追加。
広告主ごとに広告数・平均スコア・大ヒット数をカードで表示。

### 実装方針
```tsx
const [viewMode, setViewMode] = useState<"table" | "card" | "advertiser">("table");

// 広告主別集約
const advertiserStats = useMemo(() => {
  const map = new Map<string, { name: string; ads: HitAd[]; avgScore: number; megaHits: number; hits: number; totalSpend: number }>();
  hitAds.forEach(ad => {
    const name = ad.advertiser_name || "不明";
    if (!map.has(name)) {
      map.set(name, { name, ads: [], avgScore: 0, megaHits: 0, hits: 0, totalSpend: 0 });
    }
    const entry = map.get(name)!;
    entry.ads.push(ad);
    if (ad.hit_level === "mega_hit") entry.megaHits++;
    if (ad.hit_level === "hit") entry.hits++;
    entry.totalSpend += ad.spend_increase || 0;
  });
  // 平均スコア計算
  map.forEach(entry => {
    entry.avgScore = entry.ads.length > 0
      ? Math.round(entry.ads.reduce((s, a) => s + (a.hit_score || 0), 0) / entry.ads.length)
      : 0;
  });
  return Array.from(map.values()).sort((a, b) => b.avgScore - a.avgScore);
}, [hitAds]);
```

### 広告主カードUI
```tsx
{viewMode === "advertiser" && (
  <div className="grid grid-cols-2 lg:grid-cols-3 gap-3">
    {advertiserStats.map(adv => (
      <div key={adv.name} className="card px-4 py-3 cursor-pointer hover:shadow-md transition-shadow">
        <p className="text-[13px] font-bold text-gray-900 truncate">{adv.name}</p>
        <div className="flex items-center gap-2 mt-1">
          <span className="text-[22px] font-bold text-[#4A7DFF]">{adv.avgScore}</span>
          <span className="text-[10px] text-gray-400">avg score</span>
        </div>
        <div className="flex items-center gap-2 mt-1.5 flex-wrap">
          <span className="text-[10px] text-gray-500">{adv.ads.length}件</span>
          {adv.megaHits > 0 && <span className="text-[9px] px-1.5 py-0.5 rounded bg-red-100 text-red-700">大HIT {adv.megaHits}</span>}
          {adv.hits > 0 && <span className="text-[9px] px-1.5 py-0.5 rounded bg-orange-100 text-orange-700">HIT {adv.hits}</span>}
        </div>
        <p className="text-[10px] text-gray-400 mt-1">{formatYen(adv.totalSpend)}</p>
      </div>
    ))}
  </div>
)}
```

---

## コンフリクト防止ルール
- `INSTRUCTIONS.md` を厳守
- バックエンドファイルは一切触らない
- 外部ライブラリの追加不可（Chart.js, Recharts等は使わない → CSS + div のみ）
- 既存のimport（fetchApi, formatNumber, formatYen, genreOptions等）を活用
- `useMemo` を適切に使ってパフォーマンス維持
- 修正後に `cd C:/Users/ishit/ads_library/frontend && npx next build --no-lint` でビルド確認

## 完了条件
- 4タスク全ての実装が完了
- TypeScript型エラーがゼロ
- ビルドが通ること
